services:
  - type: web
    name: stock-pattern-search
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python download_cache.py
    startCommand: gunicorn backend:app --workers 1 --timeout 300 --bind 0.0.0.0:$PORT
